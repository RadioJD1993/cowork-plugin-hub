# Scripts

Put optional helper scripts here only when the plugin needs local processing that cannot be expressed clearly in Markdown instructions.

Scripts must not contain credentials, private paths, or organization-specific data. Prefer environment variables and documented setup steps.
